type Direction = 'north' | 'south' | 'east' | 'west';
const directionValues = ['north', 'south', 'east', 'west'];

type Coordinates = { x: number, y: number };

export type { Coordinates, Direction };
export { directionValues };
